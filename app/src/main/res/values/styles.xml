<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="android:statusBarColor">@android:color/white</item> <!-- Change to match your app's background -->
        <item name="android:windowLightStatusBar">true</item> <!-- Light icons on white background -->
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>
</resources>